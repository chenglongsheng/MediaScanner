# 车载媒体扫描系统 产品需求文档 (PRD)

> **文档版本**: V1.1  
> **文档状态**: 开发评审已修订  
> **作者**: -  
> **最后更新**: 2026-07-11  
> **保密等级**: 内部  

---

## 变更记录

| 版本   | 日期       | 作者 | 变更内容                         |
| ------ | ---------- | ---- | -------------------------------- |
| V0.1   | 2026-07-11 | -    | 初稿                             |
| V1.0   | 2026-07-11 | -    | 完善需求场景、功能规格、验收标准 |
| V1.1   | 2026-07-11 | -    | 开发评审修订: 性能数据标注为预期目标；USB重插改为增量扫描+卷识别；格式支持改为系统API检测+自定义配置；签名降级为signatureOrSystem+白名单；并发改为单线程时间片轮转；移除:scanner子进程 |

---

## 1. 需求背景与问题分析

### 1.1 业务背景

Android 车载系统（Android Automotive OS）已成为各大车厂智能座舱的核心操作系统。用户在车载场景中对本地媒体（USB 音乐、U 盘视频、SD 卡图片）的播放需求占比极高：

- 根据车载行业调研，超过 **65%** 的车主习惯通过 USB 设备在车内播放音乐
- 车载 USB 存储容量通常在 **32GB ~ 256GB** 之间，文件量级在 **2000 ~ 50000 个** 不等
- 用户一上车最直观的体验就是：插上 U 盘后能否 **立刻** 听歌

### 1.2 核心痛点

#### 痛点一：原生扫描速度无法接受

| 场景             | 原生 MediaScanner 表现（行业经验值）      | 用户感知         |
| ---------------- | --------------------------------------- | ---------------- |
| USB 3000 个文件  | 扫描耗时 **6 分钟以上**                  | 上车等半天才能放歌 |
| USB 10000 个文件 | 扫描耗时 **15 分钟以上**，期间 UI 可能卡顿 | 完全不可接受     |
| USB 50000 个文件 | 可能出现 ANR，甚至扫描超时失败            | 功能不可用       |

> **根因**: 原生 MediaProvider 采用单线程扫描队列，所有挂载卷的扫描任务串行排队，且逐文件解析元数据的 IO 开销未做优化。
>
> ⚠️ **提醒**: 以上原生耗时数据来源于车载行业经验值和公开技术文章，本项目需在目标车机上实测原生基线，作为性能对比的客观依据。建议在实验设计章节（见 3.3）中明确测试条件。

#### 痛点二：车机供应商裁剪不一致

不同车厂/供应商对 Android Framework 层 MediaScannerService 和 MediaProvider 的裁剪程度不同，导致：

- 上层播放器应用在不同车型上行为不一致
- 应用开发方需要为每个车型做适配，**平台迁移成本极高**
- 部分车型甚至在 Framework 层完全移除了媒体扫描能力

#### 痛点三：多 USB 场景无法并行处理

车载场景常见多 USB 同时接入（如：一个音乐 U 盘 + 一个视频 U 盘），原生方案只能串行扫描，后插入的设备需等待前一个设备扫描完成。

### 1.3 解决方案定位

开发一个**系统签名级独立媒体扫描应用**（`/system/priv-app/MediaScanner/`），自建数据库完全替代原生 MediaStore，对外提供兼容原生字段的 ContentProvider + AIDL 双通道 API。扫描采用单线程时间片轮转调度，通过 Kotlin Coroutines 将 IO 异步化。生态应用通过 `signatureOrSystem` + 包名白名单授权接入。

---

## 2. 目标用户与角色

### 2.1 直接用户

| 角色             | 描述                                                   | 核心诉求                               |
| ---------------- | ------------------------------------------------------ | -------------------------------------- |
| **车主/驾驶员**  | 使用车载娱乐系统的最终用户                             | 插上 U 盘立刻播放，操作流畅不卡顿      |
| **车载多媒体应用** | 播放器、相册、文件管理器等生态应用                     | 稳定获取媒体数据，API 兼容原生 MediaStore |
| **车厂系统集成方**  | 将本方案集成到车载 ROM 中的工程团队                    | 裁剪成本低，稳定性好，不引入系统风险    |
| **测试/QA**       | 质量验证人员                                           | 可复现的验收标准，清晰的异常定义        |

### 2.2 间接用户

| 角色           | 影响                                             |
| -------------- | ------------------------------------------------ |
| **产品经理**   | 车载娱乐体验是车型产品竞争力的重要维度             |
| **OTA 运维团队** | 要求服务在升级后自动恢复，无需人工干预            |

---

## 3. 产品目标与成功指标

### 3.1 核心目标

| 目标                       | 描述                                                         |
| -------------------------- | ------------------------------------------------------------ |
| **扫描性能 10x 提升**      | 10000 文件扫描从原生 15 分钟降至 < 5 分钟（目标 < 3 分钟）   |
| **多卷并行扫描**           | 支持 ≥ 2 个 USB 设备通过时间片轮转交替扫描，互不阻塞，无 IO 竞争 |
| **零成本生态接入**         | 播放器/相册仅替换 Authority 即可从原生 MediaStore 迁移       |
| **车载级稳定性**           | 24 小时 Monkey 测试 0 崩溃，100 次插拔 0 数据残留            |
| **灵活扫描策略**           | 支持全量/增量/按路径扫描多种策略，满足不同场景                |

### 3.2 量化指标 (KPI)

| 指标                          | 目标值            | 测量方式                   |
| ----------------------------- | ----------------- | -------------------------- |
| 1000 文件全量扫描耗时          | < 30 秒           | USB 自动化测试脚本         |
| 10000 文件全量扫描耗时         | < 3 分钟          | USB 自动化测试脚本         |
| 50000 文件扫描内存峰值         | < 50 MB           | Android Profiler           |
| USB 插入到扫描开始延迟         | < 3 秒            | 日志时间戳差值             |
| USB 拔出到数据清除完成         | < 5 秒            | 日志时间戳差值             |
| ContentProvider 单次查询(<10000条) | < 200ms           | Room Query Profiler        |
| 连续插拔稳定性                | 100 次无异常      | 自动化测试                 |
| 扫描过程中 UI 线程 ANR          | 0 次              | StrictMode + 自动化测试    |
| 应用接入代码修改量             | ≤ 10 行           | 代码审查                   |

### 3.3 实验设计与测试条件

> ⚠️ **开发评审关注点**: 以下性能指标为**设计目标**，尚未经过实测验证。需在开发阶段补全实验设计并产出实际性能报告。

| 实验项               | 测试条件                                                     | 基线采集           | 本方案目标       |
| -------------------- | ------------------------------------------------------------ | ------------------ | ---------------- |
| 扫描吞吐量           | USB 2.0 接口，FAT32 格式，文件大小混合（1~50 MB），关闭后台任务 | 原生 MediaScanner  | < 3 分钟 / 10000 文件 |
| 多卷并发             | 2 个 USB 3.0 同时插入，各 5000 文件                          | 原生串行耗时       | 总耗时 ≈ max(卷1, 卷2) 而非 sum |
| 增量扫描             | 10000 文件基线已扫描，修改其中 200 个文件                     | 原生态全量扫描     | < 15 秒（仅处理变更） |
| 内存占用             | 50000 文件全量扫描，Android Profiler 监控                    | 原生 ~80 MB        | < 50 MB          |
| 插拔稳定性           | 自动化脚本连续插拔 100 次，USB 规格混合（FAT32/exFAT/NTFS）  | -                  | 0 崩溃, 0 数据残留 |

> **实验工具**: USB 自动化测试脚本（Python + ADB）、Android Profiler、Perfetto Trace、Monkey

---

## 4. 功能需求

### 4.1 功能需求全景

| 编号    | 功能模块              | 优先级 | 说明                           |
| ------- | --------------------- | ------ | ------------------------------ |
| **F01** | 多存储卷管理           | P0     | 管理内部存储、USB、SD 卡等卷   |
| **F02** | 全量扫描引擎           | P0     | 遍历目录、识别媒体类型、提取元数据 |
| **F03** | 增量扫描               | P0     | 基于时间戳的增量更新，降低重复开销 |
| **F04** | USB/SD 热插拔响应      | P0     | 实时监听插拔事件，自动触发/取消扫描 |
| **F05** | 开机恢复               | P0     | 重启后自动恢复上次扫描状态与数据 |
| **F06** | ContentProvider API    | P0     | 对外提供媒体查询接口，兼容 MediaStore 字段 |
| **F07** | AIDL 扫描控制 API      | P1     | 提供扫描控制、进度回调、状态查询 |
| **F08** | 多卷并行扫描           | P0     | 多个 USB 同时插入时独立并行扫描 |
| **F09** | 指定路径扫描           | P1     | 客户端可指定某一路径触发扫描   |
| **F10** | 扫描进度通知           | P1     | AIDL 回调实时通知扫描进度       |
| **F11** | 定时增量刷新           | P1     | 后台定期（默认30分钟）增量更新缓存数据 |
| **F12** | 扫描暂停/恢复/取消     | P2     | 用户可在扫描过程中暂停或取消    |
| **F13** | 封面图片缓存管理       | P2     | 音频/视频封面提取及本地缓存    |
| **F14** | 按媒体类型过滤查询     | P1     | 支持按音频/视频/图片分类查询   |
| **F15** | 按存储卷过滤查询       | P1     | 支持按卷 ID 查询指定 USB 的媒体 |
| **F16** | 损坏文件容错           | P0     | 单个文件解析失败不影响整体扫描 |
| **F17** | 文件校验               | P2     | 通过 magic bytes 校验文件真实类型 |

### 4.2 详细功能规格

#### F01: 多存储卷管理

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 系统需管理所有挂载的存储卷（内部存储、USB 设备、SD 卡），为每个卷维护独立的状态与扫描记录。通过卷身份标识（UUID / serial / filesystem UUID）识别同一设备，支撑"拔插同设备免全量扫描"优化 |
| **输入**  | 系统存储广播（`MEDIA_MOUNTED` / `MEDIA_UNMOUNTED` / `MEDIA_EJECT` / `MEDIA_BAD_REMOVAL`） |
| **处理**  | 1. 识别卷类型（INTERNAL / USB / SDCARD）<br>2. 提取卷身份标识（优先级: 文件系统 UUID > 设备 serial > 分区 GUID）<br>3. 创建或恢复 Volume 对象并持久化数据库<br>4. 维护卷状态机（MOUNTED → SCANNING → READY / FAILED）<br>5. 去重 - 相同身份标识 + 相同挂载路径不重复注册 |
| **输出**  | Volume 列表通过 ContentProvider URI `content://.../storage` 可查询 |
| **卷身份模型** | `volume_uuid` + `mount_point` 联合唯一键。USB 拔出后 Volume 记录**软删除**（标记为 REMOVED，保留 scan_snapshot），同一设备重新插入时恢复为 ACTIVE 并触发增量扫描 |
| **异常处理** | 异常拔出（`MEDIA_BAD_REMOVAL`）: 清理该卷运行时缓存，但保留数据库 Volume 记录和 scan_snapshot（用于后续增量恢复），记录异常日志 |

#### F02: 全量扫描引擎

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 广度优先遍历目标目录树，通过系统 API + 配置文件双重机制识别并解析所有支持的媒体文件。格式支持不依赖硬编码列表，运行时动态检测系统能力 |
| **支持格式** | **默认**: 通过 `MediaMetadataRetriever.canHandle()` + `MimeTypeMap.getFileExtensionFromUrl()` 运行时检测系统支持的格式<br>**出厂预置**（可被系统 API 覆盖）: 音频: MP3/AAC/FLAC/WAV/OGG/WMA · 视频: MP4/AVI/MKV/MOV/WMV/FLV · 图片: JPEG/PNG/BMP/GIF/WebP/HEIF<br>**自定义扩展**: 通过 `/system/etc/media_scanner_formats.json` 配置文件新增格式（如特殊 APE/CUE/TS 等车厂专属格式），配置项包括 `extension`、`mimeType`、`mediaType`、`metadataExtractor` 类名 |
| **元数据提取策略** | 1. 优先使用 `MediaMetadataRetriever` (系统 API，支持格式自动适配)<br>2. 配置文件自定义格式使用指定 `metadataExtractor` 插件<br>3. 兜底: 文件名+扩展名推断基本信息（title = 文件名去扩展名） |
| **元数据字段** | 音频: title / artist / album / genre / duration / bitrate / sampleRate / channels<br>视频: title / duration / width / height / frameRate / rotation<br>图片: width / height / mimeType / orientation |
| **遍历策略** | BFS 广度优先，自动跳过隐藏目录、缓存目录（`.thumbnails`、`Android`、`cache` 等） |
| **解析超时** | 单文件解析超时 5 秒，超时则跳过并记录 warning 日志            |
| **批量写入** | 每 100 条记录批量写入数据库，减少 IO 次数                     |

#### F03: 增量扫描

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 基于文件修改时间戳（`date_modified`），仅处理变化的文件，大幅降低重复扫描开销 |
| **三方比对逻辑** | - **新增**: 磁盘存在, DB不存在 → 解析 + 插入<br>- **修改**: 磁盘存在, DB存在, mtime不同 → 重新解析 + 更新<br>- **无变化**: 磁盘存在, DB存在, mtime相同 → 跳过（占大多数）<br>- **已删除**: 磁盘不存在, DB存在 → 从DB删除 |
| **触发时机** | 1. 应用启动时检查所有 READY 状态的卷<br>2. 定时任务（默认 30 分钟间隔）<br>3. 用户通过 AIDL 接口手动触发 |

#### F04: USB/SD 热插拔响应

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **插入处理** | 1. 识别卷并提取卷身份标识（UUID/serial）<br>2. 查数据库是否存在匹配身份 + 挂载路径的已知卷记录<br>&nbsp;&nbsp;&nbsp;&nbsp;**已知卷**: 恢复 Volume 状态为 ACTIVE，触发**增量扫描**（基于上次 scan_snapshot 做三方比对）<br>&nbsp;&nbsp;&nbsp;&nbsp;**新卷**: 创建 Volume（状态 → MOUNTED），触发**全量扫描**<br>3. 入队扫描，开始遍历解析<br>4. 完成后 Volume 状态 → READY，更新 scan_snapshot |
| **拔出处理** | 1. 查找对应 Volume<br>2. 取消正在进行的扫描任务<br>3. **数据库事务**: 标记该卷为 REMOVED（软删除，保留 scan_snapshot）→ 清理该卷媒体记录 → 通过 ContentProvider `notifyChange()` 通知客户端刷新 |
| **重新插入优化** | 同一 USB 设备（相同 UUID/serial）插入回相同挂载点时，不触发全量扫描。系统比对 scan_snapshot 中的 `date_modified` 时间戳，仅处理插入期间有变更的文件。若中间有文件新增/修改/删除，增量扫描自动覆盖。**零变更场景（文件完全未变）: 扫描耗时 < 3 秒，仅做 Volume 状态恢复和目录结构的快速校验** |
| **性能要求** | 插入至扫描开始 < 3 秒；拔出至数据清除 < 5 秒；已知卷零变更恢复 < 3 秒 |

#### F05: 开机恢复

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 设备重启后自动初始化数据库，恢复各卷的上次扫描状态，确保用户无需等待重新全量扫描 |
| **流程**  | 1. 接收 `BOOT_COMPLETED` 广播<br>2. 初始化 Room Database<br>3. 通过 `StorageManagerService.getStorageVolumes()` 获取当前挂载卷<br>4. 与 DB 中 Persistent Volume 对比（新卷触发扫描 / 已有卷恢复 READY / 已拔出卷标记 REMOVED）<br>5. 恢复未完成的扫描任务 |

#### F06: ContentProvider API

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **Authority** | `com.txzing.media.scanner.provider`                          |
| **URI 设计** | `content://.../media` - 所有媒体<br>`content://.../media/type/{audio\|video\|image}` - 按类型过滤<br>`content://.../media/{id}` - 按 ID 查询<br>`content://.../storage` - 存储卷列表<br>`content://.../scan_tasks` - 扫描任务列表 |
| **查询参数** | `type`、`volume_id`、`sort`、`min_duration`、`min_width`、`min_height` |
| **权限保护** | `signatureOrSystem` 级别 + 可配置白名单。系统签名应用和 ROM 内置应用自动授权，第三方媒体应用可通过包名白名单配置授权（白名单路径: `/system/etc/media_scanner_allowed_packages.xml`） |

#### F07: AIDL 扫描控制 API

| 接口方法               | 描述                           |
| ---------------------- | ------------------------------ |
| `scan(path)`           | 扫描指定路径                   |
| `scanVolume(volumeId)` | 扫描指定存储卷                 |
| `cancelScan(volumeId)` | 取消指定卷的扫描               |
| `cancelAllScans()`     | 取消所有进行中的扫描           |
| `getScanState()`       | 获取当前扫描服务状态           |
| `getActiveTasks()`     | 获取活跃任务列表               |
| `registerCallback()`   | 注册扫描进度回调               |
| `unregisterCallback()` | 取消注册扫描进度回调           |

#### F08: 多卷并行扫描

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 多个 USB 设备同时插入时，各自创建独立的扫描任务，通过单线程时间片轮转机制实现"伪并行"扫描，避免多线程竞争 IO 总线 |
| **并发策略** | **单 Worker 线程 + 时间片轮转调度**。类似 CPU 时间片轮转：<br>1. 所有待扫描卷的扫描任务进入优先级队列<br>2. 单 Worker 线程按时间片（默认 5 秒）交替服务各卷扫描<br>3. 每个时间片内按 BFS 顺序处理该卷的 N 个文件，时间片到期后保存上下文并切换到下一卷<br>4. 所有卷看起来"同时"在进行，实际是时间片交替推进<br>**优势**: 无锁设计、无 IO 竞争、上下文切换开销可预测、调试简单 |
| **隔离性** | 每卷使用独立的 `volume_id` + 独立的扫描上下文（当前目录栈、已处理计数、进度偏移），一个卷的失败不影响其他卷 |
| **优先级** | 用户触发的卷（刚插入的 USB）优先级 > 定时刷新 > 手动指定路径

#### F09: 指定路径扫描

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 客户端应用（如文件管理器）可通过 AIDL `scan(path)` 方法，触发对某个特定目录的扫描 |
| **场景**  | 用户打开文件管理器某个文件夹 → 应用请求扫描该文件夹 → 快速返回该路径下的媒体文件 |
| **约束**  | 扫描结果仍按 `volume_id` 归属，与全量扫描数据合并去重         |

#### F11: 定时增量刷新

| 项        | 说明                                                         |
| --------- | ------------------------------------------------------------ |
| **描述**  | 服务端后台定期对所有 READY 状态的存储卷执行增量扫描，确保缓存数据与磁盘一致 |
| **周期**  | 默认 30 分钟，可通过配置文件调整                              |
| **策略**  | 使用 WorkManager `PeriodicWorkRequest` 实现，系统自动省电调度 |
| **约束**  | 1. 仅对 READY 状态的卷执行<br>2. 如该卷正在扫描中则跳过本次<br>3. 仅在设备充电或电量 > 50% 时执行（车载场景: 发动机运行中） |

---

## 5. 非功能需求

### 5.1 性能需求

| 编号   | 项目               | 指标要求                       |
| ------ | ------------------ | ------------------------------ |
| NF-P01 | 扫描吞吐量         | > 50 文件/秒（USB 2.0 环境）   |
| NF-P02 | 数据库查询响应     | < 200ms（10000 条记录）        |
| NF-P03 | 扫描内存峰值       | < 50 MB                        |
| NF-P04 | CPU 占用率         | 扫描期间 < 30%（单核），通过时间片粒度（默认 5s）控制 CPU 配额 |
| NF-P05 | 启动时间           | 冷启动至服务就绪 < 2 秒        |
| NF-P06 | 进程保活           | 扫描期间前台服务，不被 LMK 回收 |

### 5.2 稳定性需求

| 编号   | 项目               | 指标要求                               |
| ------ | ------------------ | -------------------------------------- |
| NF-S01 | 崩溃率             | 24 小时连续运行 0 崩溃（Monkey 测试）   |
| NF-S02 | 连续插拔           | 100 次 USB 插拔无数据残留、无内存泄漏  |
| NF-S03 | 损坏文件容错       | 目录中 50% 文件损坏不影响正常文件扫描   |
| NF-S04 | USB 意外拔出       | 扫描过程中拔出 USB，不崩溃、不脏数据    |
| NF-S05 | 磁盘满保护         | 磁盘空间不足时，优雅降级停止写入并通知  |
| NF-S06 | 进程崩溃恢复       | WorkManager 自动重试，最多 3 次         |

### 5.3 兼容性需求

| 编号   | 项目               | 指标要求                             |
| ------ | ------------------ | ------------------------------------ |
| NF-C01 | Android 版本       | 最低支持 API 28 (Android 9.0)        |
| NF-C02 | MediaStore 兼容    | 核心字段名对齐 MediaStore，迁移 ≤ 10 行代码变更 |
| NF-C03 | 文件系统支持       | FAT32 / exFAT / NTFS / ext4          |
| NF-C04 | 签名兼容           | 系统签名应用和 ROM 内置应用自动授权；第三方媒体应用通过包名白名单配置授权，无需系统签名 |

### 5.4 安全需求

| 编号   | 项目               | 要求                                         |
| ------ | ------------------ | -------------------------------------------- |
| NF-SE01 | 访问控制          | ContentProvider 和 AIDL Service 为 `signatureOrSystem` 级别保护，额外支持包名白名单机制。白名单通过 `/system/etc/media_scanner_allowed_packages.xml` 配置，允许车厂预置的第三方媒体应用（如 QQ 音乐车机版）访问 |
| NF-SE02 | 部署隔离          | 独立 APK + 独立进程，扫描任务全部异步化（Kotlin Coroutines + Dispatchers.IO），与调用方应用天然进程隔离，崩溃不影响其他应用 |
| NF-SE03 | 文件访问          | 作为系统 priv-app，不向第三方暴露文件路径     |
| NF-SE04 | 数据隔离          | USB 拔出后该卷缓存数据清理 |
| NF-SE05 | 白名单管理        | 包名白名单配置文件只读，ROM 编译时固化。运行时非法修改在下次启动时自动还原。白名单变更需通过 OTA 升级 |

---

## 6. 范围边界

### 6.1 本期范围 (In-Scope)

- ✅ 内部存储 + USB + SD 卡多卷管理
- ✅ 音频 / 视频 / 图片三类型全量 + 增量扫描
- ✅ USB/SD 热插拔自动响应
- ✅ ContentProvider + AIDL 双通道 API（兼容 MediaStore 字段）
- ✅ 开机自启动与扫描状态恢复
- ✅ 系统 priv-app 签名部署
- ✅ 定时后台增量刷新
- ✅ 扫描进度回调通知
- ✅ 按类型/按卷的查询过滤

### 6.2 本期不做 (Out-of-Scope)

- ❌ 修改 Android Framework 层
- ❌ 网络流媒体 / DLNA / SMB 网络存储索引
- ❌ DRM 保护内容的元数据解析
- ❌ 云端同步或远程数据库
- ❌ 用户自定义封面 / 标签编辑（只读）
- ❌ 播放能力（本项目仅提供索引数据，不播放）
- ❌ 文件转码 / 格式转换
- ❌ 蓝牙设备文件浏览
- ❌ iPod / iPhone USB 连接协议解析

### 6.3 未来规划 (Future Roadmap)

| 优先级 | 功能                       | 说明                             |
| ------ | -------------------------- | -------------------------------- |
| P1     | 歌词文件关联索引           | 扫描同目录 `.lrc` 文件并关联     |
| P2     | 网络存储（SMB/NFS）索引     | 支持 NAS 等局域网媒体库           |
| P2     | 媒体收藏/评分持久化         | 用户标记收藏或评分同步到数据库   |
| P3     | AI 封面生成                | 无封面音频自动生成可视化封面     |
| P3     | 语音搜索接口               | 提供语音搜索媒体文件的 API       |

---

## 7. 用户故事

| 编号 | 角色         | 故事                                                         | 验收标准                                                     |
| ---- | ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| US01 | 车主         | 我插入存有 5000 首音乐的 U 盘后，系统在 2 分钟内完成扫描，我可以立刻按歌手/专辑浏览和播放 | 5000 文件扫描 < 2 分钟，播放器可查询并播放                   |
| US02 | 车主         | 我同时插入音乐 U 盘和视频 U 盘，两个设备的扫描交替推进，我可以逐步浏览两个 U 盘的内容 | 双 USB 时间片轮转扫描，各自进度独立推进，UI 同时更新两个卷的进度 |
| US03 | 车主         | 我拔出 U 盘后，播放器列表自动清除该 U 盘的音乐，不显示已拔出的无效条目 | 拔出后 5 秒内播放器列表刷新                                   |
| US04 | 车主         | 我更换了 U 盘中的几首歌后重新插入，系统只扫描变化的部分，速度很快 | 增量扫描仅处理变化文件，50 首变化 < 10 秒                       |
| US05 | 车载应用开发者 | 我的播放器原本使用 MediaStore 查询，只需修改 Authority 和包名，即可无缝切换到新的扫描服务 | 代码变更 ≤ 10 行，查询结果字段一致                           |
| US06 | 车厂集成工程师 | 移植到新车型时，只需将 APK 和权限文件放到 priv-app 目录，无需修改 Framework 层 | 部署即用，不需要重新编译系统镜像                             |
| US07 | 测试工程师   | 我可以用自动化脚本连续插拔 U 盘 100 次，系统不崩溃且无数据残留 | 100 次插拔通过，日志无异常                                    |

---

## 8. 风险分析

| 编号 | 风险项                     | 影响等级 | 可能性 | 缓解措施                                                     |
| ---- | -------------------------- | -------- | ------ | ------------------------------------------------------------ |
| R01  | USB IO 竞争导致系统卡顿    | 高       | 中     | 单线程时间片轮转天然消除 IO 竞争；时间片粒度可配置以控制 CPU 占用 |
| R02  | 数据库损坏导致全部数据丢失 | 高       | 低     | `fallbackToDestructiveMigration()` 自动重建 + 自动重新扫描    |
| R03  | 不同供应商裁剪行为不一致   | 中       | 低     | 不依赖 Framework 层 MediaScanner，自建独立数据库              |
| R04  | 海量文件导致 ANR           | 中       | 中     | 所有 IO 异步执行；前台服务保活；大容量设备暂停策略              |
| R05  | ROM 存储空间不足           | 低       | 低     | 预估 50000 文件数据库 < 20 MB；监控剩余空间                   |
| R06  | 系统升级后兼容性问题       | 中       | 低     | 最小 API 28；不依赖隐藏 API；Google Play 审核变化不敏感       |
| R07  | OTA 升级后数据丢失         | 高       | 中     | 开机自动恢复扫描；数据库不存 `/data/data/` 临时路径           |

---

## 9. 约束条件

| 约束           | 说明                                                         |
| -------------- | ------------------------------------------------------------ |
| **架构约束**   | 不修改 Android Framework 层，以独立 APK 形式运行              |
| **部署约束**   | 部署在 `/system/priv-app/`，需要系统签名                      |
| **兼容约束**   | 数据库 Schema 核心字段对齐 MediaStore，降低生态应用接入成本    |
| **进程约束**   | 独立 APK 天然独立进程，扫描任务全部在后台协程执行（`Dispatchers.IO`），ContentProvider 主线程不被阻塞。无需额外子进程 |
| **权限约束**   | 最小权限原则，仅申请扫描所需权限。API 访问使用 `signatureOrSystem` + 包名白名单保护，兼顾安全性与车载生态兼容性 |
| **平台约束**   | 最低 API 28 (Android 9.0)，覆盖主流车载系统版本               |

---

## 10. 附录

### 附录 A: 术语表

| 术语                     | 说明                                                         |
| ------------------------ | ------------------------------------------------------------ |
| **MediaScannerService**  | Android 原生媒体扫描服务，本方案要替代的组件                   |
| **MediaProvider**        | Android 原生媒体内容提供者，本方案要替代的组件                 |
| **vold**                 | Android Volume Daemon，负责存储设备的挂载/卸载和广播发送       |
| **priv-app**             | 系统特权应用，部署在 `/system/priv-app/`，拥有系统级权限       |
| **ContentProvider**      | Android 四大组件之一，用于跨进程共享数据                       |
| **AIDL**                 | Android Interface Definition Language，用于跨进程服务通信     |
| **Volume**               | 存储卷，指一个挂载的文件系统分区（如一个 U 盘）               |
| **Room**                 | Android Jetpack ORM 数据库框架                                |
| **WorkManager**          | Android Jetpack 后台任务调度框架，保证任务在进程重启后恢复     |
| **Incremental Scan**     | 增量扫描，仅处理自上次扫描以来变化的文件                       |

### 附录 B: 与原生方案对比

| 维度               | 原生 MediaScanner            | 本方案                        |
| ------------------ | ---------------------------- | ----------------------------- |
| 架构               | Framework 层内置             | 独立 APK，系统 priv-app        |
| 多卷并发           | 单队列串行                   | 单线程时间片轮转，多卷交替推进  |
| 10000 文件扫描      | 15 分钟以上                  | < 3 分钟                      |
| 供应商裁剪风险     | 高                           | 无（独立实现）                |
| 增量扫描           | 不支持（全量扫）             | 支持（基于 mtime 三方比对）    |
| 扫描控制           | 不可中断                     | 暂停/恢复/取消                 |
| 生态接入           | 直接使用                     | 替换 Authority，≤ 10 行变更    |
| 部署方式           | 随系统固件                   | `/system/priv-app/` 部署       |

### 附录 C: 参考资料

- [Android Automotive OS 官方文档](https://developer.android.com/training/cars)
- [Android Storage Access Framework](https://developer.android.com/training/data-storage)
- [Room Persistence Library](https://developer.android.com/training/data-storage/room)
- [WorkManager Guide](https://developer.android.com/topic/libraries/architecture/workmanager)
- [Android MediaMetadataRetriever](https://developer.android.com/reference/android/media/MediaMetadataRetriever)
- [CTS Media Provider Tests](https://source.android.com/compatibility/cts)

---

> **文档状态**: 待评审，评审通过后将作为开发排期和验收的唯一依据。
