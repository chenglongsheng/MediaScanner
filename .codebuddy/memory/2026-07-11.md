# 2026-07-11

- 通过 find-skills 搜索并安装了 **claude-android-ninja** Android 开发 Skill。该 Skill 已内置于 `~/.codebuddy/skills/claude-android-ninja/`，无需手动克隆，直接可用。

- 以资深车载产品经理视角完善了 **PRD.md**，从原始 13 行扩展到完整 PRD：
  - 新增需求背景与痛点分析（车载行业数据、根因分析）
  - 17 项功能需求 P0/P1/P2 分级（原始仅 5 条需求点）
  - 9 项可量化 KPI、非功能需求（性能/稳定性/兼容性/安全）
  - 7 个用户故事、7 项风险分析与缓解措施
  - 明确 In-Scope/Out-of-Scope/Future Roadmap
  - 附录：术语表、与原生 MediaScanner 对比表

- git commit `00c6cbf`: docs: 完善车载媒体扫描PRD文档，新增项目记忆文件

- **PRD V1.1 开发评审修订**（6 项修改全部落地）:
  1. 性能数据标注为"预期目标/行业经验值"，非实测结果；新增 3.3 实验设计章节
  2. USB 拔出保留卷记录（软删除+scan_snapshot），重新插入走增量而非全量扫描
  3. 格式支持改为 MediaMetadataRetriever 运行时检测 + `/system/etc/media_scanner_formats.json` 自定义扩展
  4. 签名从 `signature` 降级为 `signatureOrSystem` + 包名白名单 `/system/etc/media_scanner_allowed_packages.xml`
  5. 多卷并发从双 Worker 线程改为单 Worker + 时间片轮转调度（无锁、无 IO 竞争）
  6. 移除 `android:process=":scanner"` 子进程设计（独立 APK + Coroutines 异步已足够）
